import { StyleSheet, TouchableWithoutFeedbackBase } from "react-native";

export default StyleSheet.create({
    texto: {
        fontSize: 18,
        fontWeight: "bold",
        borderWidth: 2,
        borderColor: 'white',
        padding: 10,
    }
});